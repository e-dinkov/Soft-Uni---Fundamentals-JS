function fundamentals(number, decimals) {
    if (decimals >= 15)
      
      {
        decimals = 15;
      }
      let newNumber = parseFloat(number.toFixed(decimals))
      console.log(newNumber);
  
  
  
  
  }